---
configuration:
  provider: github-provider
  type: github
checks:
  - id: pr-should-start-with-pr
    name: check if pr title start with PR.
    severity: High
    steps:
      - path: pull_request.title
        utility: regex
        value: "/PR/"
